# Public site URL for metadata/sitemap canonical links
NEXT_PUBLIC_SITE_URL=http://localhost:8080

# Platform metadata injected by the builder/control-plane
PROJECT_ID=project-demo
VERSION=2
RUNTIME_LANE=shared

# Persistent SQLite location used by the shared lane
DATABASE_PATH=/data/db/project-demo/app.db
